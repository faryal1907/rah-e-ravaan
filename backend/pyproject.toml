[project]
name = "rah-e-ravaan-backend"
version = "0.1.0"
description = "Backend API for Rah-e-Ravaan"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "python-dotenv",
    "pydantic-settings",
    "psycopg2-binary",
    "redis",
    "qdrant-client",
    "sqlalchemy",
    "asyncpg",
]

[tool.uvicorn]
factory = false